from typing import Iterable


def apta(sample: Iterable):
    """Transform the incoming examples of labeled traces into an augmented prefix tree
    acceptor (APTA)

    Args:
        sample (Iterable): Set of example labeled traces.
    """
    return 1
